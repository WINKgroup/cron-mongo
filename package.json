{
  "name": "@winkgroup/cron-mongo",
  "version": "1.0.1",
  "description": "Cron management class with timestamp saved in mongo db",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "format": "prettier --write 'src/**/*.ts'",
    "prepare": "npm run build",
    "postversion": "git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WINKgroup/cron-mongo.git"
  },
  "keywords": [
    "cron",
    "interval",
    "mongo",
    "mongoose"
  ],
  "author": "winksrl",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/WINKgroup/cron-mongo/issues"
  },
  "homepage": "https://github.com/WINKgroup/cron-mongo#readme",
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "@types/node": "^18.11.18",
    "prettier": "^2.8.2",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@winkgroup/console-log": "^2.0.1",
    "@winkgroup/cron": "^2.0.0",
    "@winkgroup/db-mongo": "^2.0.0",
    "lodash": "^4.17.21",
    "mongoose": "^6.8.3"
  }
}
